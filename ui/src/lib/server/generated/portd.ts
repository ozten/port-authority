// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.21.12
// source: portd.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "./google/protobuf/timestamp.js";

export const protobufPackage = "portd";

export enum ReservationState {
  RESERVATION_STATE_UNSPECIFIED = 0,
  RESERVATION_STATE_PENDING = 1,
  RESERVATION_STATE_ACTIVE = 2,
  RESERVATION_STATE_FAILED = 3,
  RESERVATION_STATE_RELEASED = 4,
  UNRECOGNIZED = -1,
}

export function reservationStateFromJSON(object: any): ReservationState {
  switch (object) {
    case 0:
    case "RESERVATION_STATE_UNSPECIFIED":
      return ReservationState.RESERVATION_STATE_UNSPECIFIED;
    case 1:
    case "RESERVATION_STATE_PENDING":
      return ReservationState.RESERVATION_STATE_PENDING;
    case 2:
    case "RESERVATION_STATE_ACTIVE":
      return ReservationState.RESERVATION_STATE_ACTIVE;
    case 3:
    case "RESERVATION_STATE_FAILED":
      return ReservationState.RESERVATION_STATE_FAILED;
    case 4:
    case "RESERVATION_STATE_RELEASED":
      return ReservationState.RESERVATION_STATE_RELEASED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReservationState.UNRECOGNIZED;
  }
}

export function reservationStateToJSON(object: ReservationState): string {
  switch (object) {
    case ReservationState.RESERVATION_STATE_UNSPECIFIED:
      return "RESERVATION_STATE_UNSPECIFIED";
    case ReservationState.RESERVATION_STATE_PENDING:
      return "RESERVATION_STATE_PENDING";
    case ReservationState.RESERVATION_STATE_ACTIVE:
      return "RESERVATION_STATE_ACTIVE";
    case ReservationState.RESERVATION_STATE_FAILED:
      return "RESERVATION_STATE_FAILED";
    case ReservationState.RESERVATION_STATE_RELEASED:
      return "RESERVATION_STATE_RELEASED";
    case ReservationState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ReserveRequest {
  owner: string;
  preferredPort?: number | undefined;
  targetHost: string;
  targetPort: number;
  leaseSeconds?: number | undefined;
  exactOnly: boolean;
}

export interface ReserveResponse {
  reservationId: string;
  assignedPort: number;
  state: ReservationState;
}

export interface ReleaseRequest {
  identifier?: { $case: "reservationId"; reservationId: string } | { $case: "port"; port: number } | undefined;
}

export interface ReleaseResponse {
  success: boolean;
  message: string;
}

export interface ListRequest {
  ownerFilter?: string | undefined;
  stateFilter?: ReservationState | undefined;
}

export interface ListResponse {
  reservations: ReservationInfo[];
}

export interface InspectRequest {
  identifier?: { $case: "reservationId"; reservationId: string } | { $case: "port"; port: number } | undefined;
}

export interface InspectResponse {
  reservation?: ReservationInfo | undefined;
  tunnelHealth?: TunnelHealth | undefined;
}

export interface ReservationInfo {
  id: string;
  owner: string;
  requestedPort: number;
  assignedPort: number;
  targetHost: string;
  targetPort: number;
  state: ReservationState;
  createdAt?: Timestamp | undefined;
  updatedAt?: Timestamp | undefined;
  leaseSeconds?: number | undefined;
  expiresAt?: Timestamp | undefined;
}

export interface TunnelHealth {
  alive: boolean;
  lastCheck?: Timestamp | undefined;
  uptimeSeconds: number;
  reconnectCount: number;
}

export interface WatchRequest {
  ownerFilter?: string | undefined;
}

export interface ReservationEvent {
  reservationId: string;
  oldState: ReservationState;
  newState: ReservationState;
  timestamp?: Timestamp | undefined;
  message?: string | undefined;
}

function createBaseReserveRequest(): ReserveRequest {
  return {
    owner: "",
    preferredPort: undefined,
    targetHost: "",
    targetPort: 0,
    leaseSeconds: undefined,
    exactOnly: false,
  };
}

export const ReserveRequest: MessageFns<ReserveRequest> = {
  encode(message: ReserveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.preferredPort !== undefined) {
      writer.uint32(16).uint32(message.preferredPort);
    }
    if (message.targetHost !== "") {
      writer.uint32(26).string(message.targetHost);
    }
    if (message.targetPort !== 0) {
      writer.uint32(32).uint32(message.targetPort);
    }
    if (message.leaseSeconds !== undefined) {
      writer.uint32(40).uint32(message.leaseSeconds);
    }
    if (message.exactOnly !== false) {
      writer.uint32(48).bool(message.exactOnly);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReserveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.preferredPort = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.targetHost = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.targetPort = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.leaseSeconds = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.exactOnly = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReserveRequest {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      preferredPort: isSet(object.preferredPort)
        ? globalThis.Number(object.preferredPort)
        : isSet(object.preferred_port)
        ? globalThis.Number(object.preferred_port)
        : undefined,
      targetHost: isSet(object.targetHost)
        ? globalThis.String(object.targetHost)
        : isSet(object.target_host)
        ? globalThis.String(object.target_host)
        : "",
      targetPort: isSet(object.targetPort)
        ? globalThis.Number(object.targetPort)
        : isSet(object.target_port)
        ? globalThis.Number(object.target_port)
        : 0,
      leaseSeconds: isSet(object.leaseSeconds)
        ? globalThis.Number(object.leaseSeconds)
        : isSet(object.lease_seconds)
        ? globalThis.Number(object.lease_seconds)
        : undefined,
      exactOnly: isSet(object.exactOnly)
        ? globalThis.Boolean(object.exactOnly)
        : isSet(object.exact_only)
        ? globalThis.Boolean(object.exact_only)
        : false,
    };
  },

  toJSON(message: ReserveRequest): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.preferredPort !== undefined) {
      obj.preferredPort = Math.round(message.preferredPort);
    }
    if (message.targetHost !== "") {
      obj.targetHost = message.targetHost;
    }
    if (message.targetPort !== 0) {
      obj.targetPort = Math.round(message.targetPort);
    }
    if (message.leaseSeconds !== undefined) {
      obj.leaseSeconds = Math.round(message.leaseSeconds);
    }
    if (message.exactOnly !== false) {
      obj.exactOnly = message.exactOnly;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReserveRequest>, I>>(base?: I): ReserveRequest {
    return ReserveRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReserveRequest>, I>>(object: I): ReserveRequest {
    const message = createBaseReserveRequest();
    message.owner = object.owner ?? "";
    message.preferredPort = object.preferredPort ?? undefined;
    message.targetHost = object.targetHost ?? "";
    message.targetPort = object.targetPort ?? 0;
    message.leaseSeconds = object.leaseSeconds ?? undefined;
    message.exactOnly = object.exactOnly ?? false;
    return message;
  },
};

function createBaseReserveResponse(): ReserveResponse {
  return { reservationId: "", assignedPort: 0, state: 0 };
}

export const ReserveResponse: MessageFns<ReserveResponse> = {
  encode(message: ReserveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservationId !== "") {
      writer.uint32(10).string(message.reservationId);
    }
    if (message.assignedPort !== 0) {
      writer.uint32(16).uint32(message.assignedPort);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReserveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.assignedPort = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReserveResponse {
    return {
      reservationId: isSet(object.reservationId)
        ? globalThis.String(object.reservationId)
        : isSet(object.reservation_id)
        ? globalThis.String(object.reservation_id)
        : "",
      assignedPort: isSet(object.assignedPort)
        ? globalThis.Number(object.assignedPort)
        : isSet(object.assigned_port)
        ? globalThis.Number(object.assigned_port)
        : 0,
      state: isSet(object.state) ? reservationStateFromJSON(object.state) : 0,
    };
  },

  toJSON(message: ReserveResponse): unknown {
    const obj: any = {};
    if (message.reservationId !== "") {
      obj.reservationId = message.reservationId;
    }
    if (message.assignedPort !== 0) {
      obj.assignedPort = Math.round(message.assignedPort);
    }
    if (message.state !== 0) {
      obj.state = reservationStateToJSON(message.state);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReserveResponse>, I>>(base?: I): ReserveResponse {
    return ReserveResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReserveResponse>, I>>(object: I): ReserveResponse {
    const message = createBaseReserveResponse();
    message.reservationId = object.reservationId ?? "";
    message.assignedPort = object.assignedPort ?? 0;
    message.state = object.state ?? 0;
    return message;
  },
};

function createBaseReleaseRequest(): ReleaseRequest {
  return { identifier: undefined };
}

export const ReleaseRequest: MessageFns<ReleaseRequest> = {
  encode(message: ReleaseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.identifier?.$case) {
      case "reservationId":
        writer.uint32(10).string(message.identifier.reservationId);
        break;
      case "port":
        writer.uint32(16).uint32(message.identifier.port);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleaseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.identifier = { $case: "reservationId", reservationId: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.identifier = { $case: "port", port: reader.uint32() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleaseRequest {
    return {
      identifier: isSet(object.reservationId)
        ? { $case: "reservationId", reservationId: globalThis.String(object.reservationId) }
        : isSet(object.reservation_id)
        ? { $case: "reservationId", reservationId: globalThis.String(object.reservation_id) }
        : isSet(object.port)
        ? { $case: "port", port: globalThis.Number(object.port) }
        : undefined,
    };
  },

  toJSON(message: ReleaseRequest): unknown {
    const obj: any = {};
    if (message.identifier?.$case === "reservationId") {
      obj.reservationId = message.identifier.reservationId;
    } else if (message.identifier?.$case === "port") {
      obj.port = Math.round(message.identifier.port);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReleaseRequest>, I>>(base?: I): ReleaseRequest {
    return ReleaseRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReleaseRequest>, I>>(object: I): ReleaseRequest {
    const message = createBaseReleaseRequest();
    switch (object.identifier?.$case) {
      case "reservationId": {
        if (object.identifier?.reservationId !== undefined && object.identifier?.reservationId !== null) {
          message.identifier = { $case: "reservationId", reservationId: object.identifier.reservationId };
        }
        break;
      }
      case "port": {
        if (object.identifier?.port !== undefined && object.identifier?.port !== null) {
          message.identifier = { $case: "port", port: object.identifier.port };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseReleaseResponse(): ReleaseResponse {
  return { success: false, message: "" };
}

export const ReleaseResponse: MessageFns<ReleaseResponse> = {
  encode(message: ReleaseResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleaseResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleaseResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: ReleaseResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReleaseResponse>, I>>(base?: I): ReleaseResponse {
    return ReleaseResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReleaseResponse>, I>>(object: I): ReleaseResponse {
    const message = createBaseReleaseResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseListRequest(): ListRequest {
  return { ownerFilter: undefined, stateFilter: undefined };
}

export const ListRequest: MessageFns<ListRequest> = {
  encode(message: ListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ownerFilter !== undefined) {
      writer.uint32(10).string(message.ownerFilter);
    }
    if (message.stateFilter !== undefined) {
      writer.uint32(16).int32(message.stateFilter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ownerFilter = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stateFilter = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRequest {
    return {
      ownerFilter: isSet(object.ownerFilter)
        ? globalThis.String(object.ownerFilter)
        : isSet(object.owner_filter)
        ? globalThis.String(object.owner_filter)
        : undefined,
      stateFilter: isSet(object.stateFilter)
        ? reservationStateFromJSON(object.stateFilter)
        : isSet(object.state_filter)
        ? reservationStateFromJSON(object.state_filter)
        : undefined,
    };
  },

  toJSON(message: ListRequest): unknown {
    const obj: any = {};
    if (message.ownerFilter !== undefined) {
      obj.ownerFilter = message.ownerFilter;
    }
    if (message.stateFilter !== undefined) {
      obj.stateFilter = reservationStateToJSON(message.stateFilter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListRequest>, I>>(base?: I): ListRequest {
    return ListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListRequest>, I>>(object: I): ListRequest {
    const message = createBaseListRequest();
    message.ownerFilter = object.ownerFilter ?? undefined;
    message.stateFilter = object.stateFilter ?? undefined;
    return message;
  },
};

function createBaseListResponse(): ListResponse {
  return { reservations: [] };
}

export const ListResponse: MessageFns<ListResponse> = {
  encode(message: ListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.reservations) {
      ReservationInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservations.push(ReservationInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListResponse {
    return {
      reservations: globalThis.Array.isArray(object?.reservations)
        ? object.reservations.map((e: any) => ReservationInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListResponse): unknown {
    const obj: any = {};
    if (message.reservations?.length) {
      obj.reservations = message.reservations.map((e) => ReservationInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListResponse>, I>>(base?: I): ListResponse {
    return ListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListResponse>, I>>(object: I): ListResponse {
    const message = createBaseListResponse();
    message.reservations = object.reservations?.map((e) => ReservationInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseInspectRequest(): InspectRequest {
  return { identifier: undefined };
}

export const InspectRequest: MessageFns<InspectRequest> = {
  encode(message: InspectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.identifier?.$case) {
      case "reservationId":
        writer.uint32(10).string(message.identifier.reservationId);
        break;
      case "port":
        writer.uint32(16).uint32(message.identifier.port);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InspectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInspectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.identifier = { $case: "reservationId", reservationId: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.identifier = { $case: "port", port: reader.uint32() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InspectRequest {
    return {
      identifier: isSet(object.reservationId)
        ? { $case: "reservationId", reservationId: globalThis.String(object.reservationId) }
        : isSet(object.reservation_id)
        ? { $case: "reservationId", reservationId: globalThis.String(object.reservation_id) }
        : isSet(object.port)
        ? { $case: "port", port: globalThis.Number(object.port) }
        : undefined,
    };
  },

  toJSON(message: InspectRequest): unknown {
    const obj: any = {};
    if (message.identifier?.$case === "reservationId") {
      obj.reservationId = message.identifier.reservationId;
    } else if (message.identifier?.$case === "port") {
      obj.port = Math.round(message.identifier.port);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InspectRequest>, I>>(base?: I): InspectRequest {
    return InspectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InspectRequest>, I>>(object: I): InspectRequest {
    const message = createBaseInspectRequest();
    switch (object.identifier?.$case) {
      case "reservationId": {
        if (object.identifier?.reservationId !== undefined && object.identifier?.reservationId !== null) {
          message.identifier = { $case: "reservationId", reservationId: object.identifier.reservationId };
        }
        break;
      }
      case "port": {
        if (object.identifier?.port !== undefined && object.identifier?.port !== null) {
          message.identifier = { $case: "port", port: object.identifier.port };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseInspectResponse(): InspectResponse {
  return { reservation: undefined, tunnelHealth: undefined };
}

export const InspectResponse: MessageFns<InspectResponse> = {
  encode(message: InspectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservation !== undefined) {
      ReservationInfo.encode(message.reservation, writer.uint32(10).fork()).join();
    }
    if (message.tunnelHealth !== undefined) {
      TunnelHealth.encode(message.tunnelHealth, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InspectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInspectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservation = ReservationInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tunnelHealth = TunnelHealth.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InspectResponse {
    return {
      reservation: isSet(object.reservation) ? ReservationInfo.fromJSON(object.reservation) : undefined,
      tunnelHealth: isSet(object.tunnelHealth)
        ? TunnelHealth.fromJSON(object.tunnelHealth)
        : isSet(object.tunnel_health)
        ? TunnelHealth.fromJSON(object.tunnel_health)
        : undefined,
    };
  },

  toJSON(message: InspectResponse): unknown {
    const obj: any = {};
    if (message.reservation !== undefined) {
      obj.reservation = ReservationInfo.toJSON(message.reservation);
    }
    if (message.tunnelHealth !== undefined) {
      obj.tunnelHealth = TunnelHealth.toJSON(message.tunnelHealth);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InspectResponse>, I>>(base?: I): InspectResponse {
    return InspectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InspectResponse>, I>>(object: I): InspectResponse {
    const message = createBaseInspectResponse();
    message.reservation = (object.reservation !== undefined && object.reservation !== null)
      ? ReservationInfo.fromPartial(object.reservation)
      : undefined;
    message.tunnelHealth = (object.tunnelHealth !== undefined && object.tunnelHealth !== null)
      ? TunnelHealth.fromPartial(object.tunnelHealth)
      : undefined;
    return message;
  },
};

function createBaseReservationInfo(): ReservationInfo {
  return {
    id: "",
    owner: "",
    requestedPort: 0,
    assignedPort: 0,
    targetHost: "",
    targetPort: 0,
    state: 0,
    createdAt: undefined,
    updatedAt: undefined,
    leaseSeconds: undefined,
    expiresAt: undefined,
  };
}

export const ReservationInfo: MessageFns<ReservationInfo> = {
  encode(message: ReservationInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.owner !== "") {
      writer.uint32(18).string(message.owner);
    }
    if (message.requestedPort !== 0) {
      writer.uint32(24).uint32(message.requestedPort);
    }
    if (message.assignedPort !== 0) {
      writer.uint32(32).uint32(message.assignedPort);
    }
    if (message.targetHost !== "") {
      writer.uint32(42).string(message.targetHost);
    }
    if (message.targetPort !== 0) {
      writer.uint32(48).uint32(message.targetPort);
    }
    if (message.state !== 0) {
      writer.uint32(56).int32(message.state);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(message.createdAt, writer.uint32(66).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(message.updatedAt, writer.uint32(74).fork()).join();
    }
    if (message.leaseSeconds !== undefined) {
      writer.uint32(80).uint32(message.leaseSeconds);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(message.expiresAt, writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReservationInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservationInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.requestedPort = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.assignedPort = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.targetHost = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.targetPort = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.createdAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.updatedAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.leaseSeconds = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.expiresAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReservationInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      requestedPort: isSet(object.requestedPort)
        ? globalThis.Number(object.requestedPort)
        : isSet(object.requested_port)
        ? globalThis.Number(object.requested_port)
        : 0,
      assignedPort: isSet(object.assignedPort)
        ? globalThis.Number(object.assignedPort)
        : isSet(object.assigned_port)
        ? globalThis.Number(object.assigned_port)
        : 0,
      targetHost: isSet(object.targetHost)
        ? globalThis.String(object.targetHost)
        : isSet(object.target_host)
        ? globalThis.String(object.target_host)
        : "",
      targetPort: isSet(object.targetPort)
        ? globalThis.Number(object.targetPort)
        : isSet(object.target_port)
        ? globalThis.Number(object.target_port)
        : 0,
      state: isSet(object.state) ? reservationStateFromJSON(object.state) : 0,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
      leaseSeconds: isSet(object.leaseSeconds)
        ? globalThis.Number(object.leaseSeconds)
        : isSet(object.lease_seconds)
        ? globalThis.Number(object.lease_seconds)
        : undefined,
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
    };
  },

  toJSON(message: ReservationInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.requestedPort !== 0) {
      obj.requestedPort = Math.round(message.requestedPort);
    }
    if (message.assignedPort !== 0) {
      obj.assignedPort = Math.round(message.assignedPort);
    }
    if (message.targetHost !== "") {
      obj.targetHost = message.targetHost;
    }
    if (message.targetPort !== 0) {
      obj.targetPort = Math.round(message.targetPort);
    }
    if (message.state !== 0) {
      obj.state = reservationStateToJSON(message.state);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = fromTimestamp(message.createdAt).toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = fromTimestamp(message.updatedAt).toISOString();
    }
    if (message.leaseSeconds !== undefined) {
      obj.leaseSeconds = Math.round(message.leaseSeconds);
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = fromTimestamp(message.expiresAt).toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReservationInfo>, I>>(base?: I): ReservationInfo {
    return ReservationInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReservationInfo>, I>>(object: I): ReservationInfo {
    const message = createBaseReservationInfo();
    message.id = object.id ?? "";
    message.owner = object.owner ?? "";
    message.requestedPort = object.requestedPort ?? 0;
    message.assignedPort = object.assignedPort ?? 0;
    message.targetHost = object.targetHost ?? "";
    message.targetPort = object.targetPort ?? 0;
    message.state = object.state ?? 0;
    message.createdAt = (object.createdAt !== undefined && object.createdAt !== null)
      ? Timestamp.fromPartial(object.createdAt)
      : undefined;
    message.updatedAt = (object.updatedAt !== undefined && object.updatedAt !== null)
      ? Timestamp.fromPartial(object.updatedAt)
      : undefined;
    message.leaseSeconds = object.leaseSeconds ?? undefined;
    message.expiresAt = (object.expiresAt !== undefined && object.expiresAt !== null)
      ? Timestamp.fromPartial(object.expiresAt)
      : undefined;
    return message;
  },
};

function createBaseTunnelHealth(): TunnelHealth {
  return { alive: false, lastCheck: undefined, uptimeSeconds: 0, reconnectCount: 0 };
}

export const TunnelHealth: MessageFns<TunnelHealth> = {
  encode(message: TunnelHealth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.alive !== false) {
      writer.uint32(8).bool(message.alive);
    }
    if (message.lastCheck !== undefined) {
      Timestamp.encode(message.lastCheck, writer.uint32(18).fork()).join();
    }
    if (message.uptimeSeconds !== 0) {
      writer.uint32(24).uint32(message.uptimeSeconds);
    }
    if (message.reconnectCount !== 0) {
      writer.uint32(32).uint32(message.reconnectCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TunnelHealth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTunnelHealth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.alive = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lastCheck = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.uptimeSeconds = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.reconnectCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TunnelHealth {
    return {
      alive: isSet(object.alive) ? globalThis.Boolean(object.alive) : false,
      lastCheck: isSet(object.lastCheck)
        ? fromJsonTimestamp(object.lastCheck)
        : isSet(object.last_check)
        ? fromJsonTimestamp(object.last_check)
        : undefined,
      uptimeSeconds: isSet(object.uptimeSeconds)
        ? globalThis.Number(object.uptimeSeconds)
        : isSet(object.uptime_seconds)
        ? globalThis.Number(object.uptime_seconds)
        : 0,
      reconnectCount: isSet(object.reconnectCount)
        ? globalThis.Number(object.reconnectCount)
        : isSet(object.reconnect_count)
        ? globalThis.Number(object.reconnect_count)
        : 0,
    };
  },

  toJSON(message: TunnelHealth): unknown {
    const obj: any = {};
    if (message.alive !== false) {
      obj.alive = message.alive;
    }
    if (message.lastCheck !== undefined) {
      obj.lastCheck = fromTimestamp(message.lastCheck).toISOString();
    }
    if (message.uptimeSeconds !== 0) {
      obj.uptimeSeconds = Math.round(message.uptimeSeconds);
    }
    if (message.reconnectCount !== 0) {
      obj.reconnectCount = Math.round(message.reconnectCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TunnelHealth>, I>>(base?: I): TunnelHealth {
    return TunnelHealth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TunnelHealth>, I>>(object: I): TunnelHealth {
    const message = createBaseTunnelHealth();
    message.alive = object.alive ?? false;
    message.lastCheck = (object.lastCheck !== undefined && object.lastCheck !== null)
      ? Timestamp.fromPartial(object.lastCheck)
      : undefined;
    message.uptimeSeconds = object.uptimeSeconds ?? 0;
    message.reconnectCount = object.reconnectCount ?? 0;
    return message;
  },
};

function createBaseWatchRequest(): WatchRequest {
  return { ownerFilter: undefined };
}

export const WatchRequest: MessageFns<WatchRequest> = {
  encode(message: WatchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ownerFilter !== undefined) {
      writer.uint32(10).string(message.ownerFilter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WatchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWatchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ownerFilter = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WatchRequest {
    return {
      ownerFilter: isSet(object.ownerFilter)
        ? globalThis.String(object.ownerFilter)
        : isSet(object.owner_filter)
        ? globalThis.String(object.owner_filter)
        : undefined,
    };
  },

  toJSON(message: WatchRequest): unknown {
    const obj: any = {};
    if (message.ownerFilter !== undefined) {
      obj.ownerFilter = message.ownerFilter;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WatchRequest>, I>>(base?: I): WatchRequest {
    return WatchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WatchRequest>, I>>(object: I): WatchRequest {
    const message = createBaseWatchRequest();
    message.ownerFilter = object.ownerFilter ?? undefined;
    return message;
  },
};

function createBaseReservationEvent(): ReservationEvent {
  return { reservationId: "", oldState: 0, newState: 0, timestamp: undefined, message: undefined };
}

export const ReservationEvent: MessageFns<ReservationEvent> = {
  encode(message: ReservationEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservationId !== "") {
      writer.uint32(10).string(message.reservationId);
    }
    if (message.oldState !== 0) {
      writer.uint32(16).int32(message.oldState);
    }
    if (message.newState !== 0) {
      writer.uint32(24).int32(message.newState);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(34).fork()).join();
    }
    if (message.message !== undefined) {
      writer.uint32(42).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReservationEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservationEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.oldState = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.newState = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReservationEvent {
    return {
      reservationId: isSet(object.reservationId)
        ? globalThis.String(object.reservationId)
        : isSet(object.reservation_id)
        ? globalThis.String(object.reservation_id)
        : "",
      oldState: isSet(object.oldState)
        ? reservationStateFromJSON(object.oldState)
        : isSet(object.old_state)
        ? reservationStateFromJSON(object.old_state)
        : 0,
      newState: isSet(object.newState)
        ? reservationStateFromJSON(object.newState)
        : isSet(object.new_state)
        ? reservationStateFromJSON(object.new_state)
        : 0,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
    };
  },

  toJSON(message: ReservationEvent): unknown {
    const obj: any = {};
    if (message.reservationId !== "") {
      obj.reservationId = message.reservationId;
    }
    if (message.oldState !== 0) {
      obj.oldState = reservationStateToJSON(message.oldState);
    }
    if (message.newState !== 0) {
      obj.newState = reservationStateToJSON(message.newState);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = fromTimestamp(message.timestamp).toISOString();
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReservationEvent>, I>>(base?: I): ReservationEvent {
    return ReservationEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReservationEvent>, I>>(object: I): ReservationEvent {
    const message = createBaseReservationEvent();
    message.reservationId = object.reservationId ?? "";
    message.oldState = object.oldState ?? 0;
    message.newState = object.newState ?? 0;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.message = object.message ?? undefined;
    return message;
  },
};

export type PortBrokerDefinition = typeof PortBrokerDefinition;
export const PortBrokerDefinition = {
  name: "PortBroker",
  fullName: "portd.PortBroker",
  methods: {
    reserve: {
      name: "Reserve",
      requestType: ReserveRequest as typeof ReserveRequest,
      requestStream: false,
      responseType: ReserveResponse as typeof ReserveResponse,
      responseStream: false,
      options: {},
    },
    release: {
      name: "Release",
      requestType: ReleaseRequest as typeof ReleaseRequest,
      requestStream: false,
      responseType: ReleaseResponse as typeof ReleaseResponse,
      responseStream: false,
      options: {},
    },
    list: {
      name: "List",
      requestType: ListRequest as typeof ListRequest,
      requestStream: false,
      responseType: ListResponse as typeof ListResponse,
      responseStream: false,
      options: {},
    },
    inspect: {
      name: "Inspect",
      requestType: InspectRequest as typeof InspectRequest,
      requestStream: false,
      responseType: InspectResponse as typeof InspectResponse,
      responseStream: false,
      options: {},
    },
    watch: {
      name: "Watch",
      requestType: WatchRequest as typeof WatchRequest,
      requestStream: false,
      responseType: ReservationEvent as typeof ReservationEvent,
      responseStream: true,
      options: {},
    },
  },
} as const;

export interface PortBrokerServiceImplementation<CallContextExt = {}> {
  reserve(request: ReserveRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ReserveResponse>>;
  release(request: ReleaseRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ReleaseResponse>>;
  list(request: ListRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ListResponse>>;
  inspect(request: InspectRequest, context: CallContext & CallContextExt): Promise<DeepPartial<InspectResponse>>;
  watch(
    request: WatchRequest,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<ReservationEvent>>;
}

export interface PortBrokerClient<CallOptionsExt = {}> {
  reserve(request: DeepPartial<ReserveRequest>, options?: CallOptions & CallOptionsExt): Promise<ReserveResponse>;
  release(request: DeepPartial<ReleaseRequest>, options?: CallOptions & CallOptionsExt): Promise<ReleaseResponse>;
  list(request: DeepPartial<ListRequest>, options?: CallOptions & CallOptionsExt): Promise<ListResponse>;
  inspect(request: DeepPartial<InspectRequest>, options?: CallOptions & CallOptionsExt): Promise<InspectResponse>;
  watch(request: DeepPartial<WatchRequest>, options?: CallOptions & CallOptionsExt): AsyncIterable<ReservationEvent>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Timestamp {
  if (o instanceof globalThis.Date) {
    return toTimestamp(o);
  } else if (typeof o === "string") {
    return toTimestamp(new globalThis.Date(o));
  } else {
    return Timestamp.fromJSON(o);
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
